# XLNF — legacy GitHub Pages location.
# The canonical site is https://xlnf.dev/ and every page here declares a
# rel=canonical pointing at its equivalent there.
#
# Crawling is deliberately still allowed: blocking it would stop crawlers from
# ever reading those canonical tags, which is how the move gets consolidated.

User-agent: *
Allow: /

Sitemap: https://xlnf.dev/sitemap.xml
